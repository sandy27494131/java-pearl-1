package com.robert.dsal.recursion.condiv;

/**
 * 
 * Implement a continuous math division. For example, 100 / 2 / 2 / 2 / 30
 * 
 */
public interface ContineousDiv {
	public double divContineously(double[] operants);
}
